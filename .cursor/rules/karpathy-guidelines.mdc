---
description: 编码哲学原则
alwaysApply: true
---
# 编码哲学（Karpathy Rules）
## 核心原则
1. **简单优先**：能用 50 行解决的问题，不写 200 行
2. **先跑通，再优化**：POC 阶段不做过度抽象
3. **显式优于隐式**：配置项明确写出，不依赖魔法默认值
4. **错误要响**：验证失败立即抛出，不静默吞掉异常
5. **可测试性**：services 层不依赖框架，可直接 pytest
## AI 辅助开发原则
- 生成代码后立即运行验证，不假设 AI 代码正确
- SymPy 验证是数学正确性的唯一可信来源，不信任 LLM 的数学计算
- 每个新功能先写最小可验证版本，再迭代
